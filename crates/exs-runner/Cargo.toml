[package]
name = "exs-runner"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[features]
default = ["server"]
server = ["dep:wasmtime"]
browser = ["dep:js-sys", "dep:wasm-bindgen", "dep:wasm-bindgen-futures"]

[dependencies]
exs-abi = { path = "../exs-abi" }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
wasmtime = { workspace = true, optional = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
js-sys = { version = "0.3.103", optional = true }
wasm-bindgen = { version = "0.2.106", optional = true }
wasm-bindgen-futures = { version = "0.4.56", optional = true }

[dev-dependencies]
exs-runtime = { path = "../exs-runtime" }
exs-compiler = { path = "../exs-compiler" }
exs-value = { path = "../exs-value" }

[[bench]]
name = "basic_script"
harness = false
